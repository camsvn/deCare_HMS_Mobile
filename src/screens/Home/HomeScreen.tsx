import React from 'react';
import {Text} from 'native-base';
import {SafeAreaView, Text as NText} from 'react-native';

const HomeScreenComponent = () => {
  return (
    <SafeAreaView>
      <Text>Home Screen</Text>
    </SafeAreaView>
  );
};

export default HomeScreenComponent;
